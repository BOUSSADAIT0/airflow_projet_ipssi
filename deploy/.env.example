# Copiez en .env à la racine : cp deploy/.env.example .env
# NE PAS commiter .env (il contient le token DuckDNS).

# Domaine DuckDNS
PUBLIC_HOST=lecture-facture.duckdns.org

# Token DuckDNS (pour mettre à jour l'IP automatiquement via deploy/update-duckdns.sh)
# À récupérer sur https://www.duckdns.org (Account → token)
DUCKDNS_TOKEN=votre_token_ici

# Grafana derrière la gateway (optionnel) : à activer si vous lancez avec bigdata + gateway
# Décommentez et adaptez l’URL (port 8888) pour que le lien « Grafana (Monitoring) » fonctionne.
# GRAFANA_ROOT_URL=http://lecture-facture.duckdns.org:8888/grafana
# GRAFANA_SERVE_FROM_SUB_PATH=true
