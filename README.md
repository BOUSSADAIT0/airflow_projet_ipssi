# Apache Airflow – Pack Notebooks & Lab (M2)

Généré le 2026-01-22.

## Démarrage rapide (recommandé)
Dans ce dossier :

```bash
docker compose up -d
```

UI Airflow : http://localhost:8080  
Identifiants : **admin / admin**

Mock API (exercices Jour 3) : http://localhost:8099/health

## Structure
- `notebooks/` : 5 notebooks (Jour 1→5)
- `dags/` : exemples de DAGs
- `data/` : données de TP
- `scripts/` : scripts utilitaires (mock API)
